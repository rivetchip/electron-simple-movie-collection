{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "identifier": "meson:dev",
            "label": "Meson dev",
            "type": "shell",
            "command": "meson -Ddeveloper_mode=true $builddir && ninja -C $builddir",
            "options": {
                "env": {
                    "builddir": "devbuild"
                },
                "cwd": "${workspaceFolder}/webkit2gtk/",
            }
        },
        {
            "identifier": "meson:release",
            "label": "Meson release",
            "type": "shell",
            "command": "meson $builddir && ninja -C $builddir",
            "options": {
                "env": {
                    "builddir": "releasebuild"
                },
                "cwd": "${workspaceFolder}/webkit2gtk/",
            }
        },
    ]
}